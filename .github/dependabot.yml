version: 2
updates:
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
  # The images the end-to-end tests build. A gomod entry at "/" does not
  # reach a Dockerfile, and a docker entry at "/" does not reach a subdirectory.
  - package-ecosystem: "docker"
    directory: "/tests/e2e"
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
